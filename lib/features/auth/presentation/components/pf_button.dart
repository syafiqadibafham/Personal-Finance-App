import 'package:flutter/material.dart';

class PfButton extends StatelessWidget {
  const PfButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
